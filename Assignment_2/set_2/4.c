#include<stdio.h>
int main()
{
    float taxRate;
    long int salary, rent, savings;

    scanf("%ld, %ld, %ld", &salary, &rent, &savings);

    printf("\n%ld, %ld, %ld", salary, rent, savings);

    return (0);
}